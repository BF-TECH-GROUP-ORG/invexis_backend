# websocket-service/.env.example (updated for cluster)
PORT=9002
NODE_ENV=development
CLUSTER_WORKERS=4  # Number of workers (CPU cores)

# Redis & RabbitMQ are shared via /app/shared - no env needed

# CORS origins for clients
CORS_ORIGINS=http://localhost:8000,http://localhost:8001,http://localhost:8003,http://localhost:8004,http://localhost:8005,http://localhost:8006,http://localhost:8007,http://localhost:8008,http://localhost:8009,http://localhost:9000,http://localhost:9001,http://localhost:9002
JWT_SECRET=sdjnjkdjafd8a79d7fa76yuadsjbjahsgtd76y3498hnjf//dkjsfa
# Load balancer (if behind Nginx/Haproxy)
LB_STICKY_SESSIONS=false  # Set to true if using sticky sessions